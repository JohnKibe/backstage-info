apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: cellulant-gitlab
  description: |
    GitLab service
  labels:
    system: gitlab
    lifecycle: production
  annotations:
    gitlab.com/project-slug: 'ops-templates'
spec:
  lifecycle: production
  owner: cicd
  type: service
